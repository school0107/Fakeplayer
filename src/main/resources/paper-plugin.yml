name: FakePlayerFolia
version: '1.0'
main: com.example.fakeplayerfolia.FakePlayerFolia
api-version: '1.21'
folia-supported: true
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: true